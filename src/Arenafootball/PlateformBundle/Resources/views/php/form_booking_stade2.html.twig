
<div id="form_search" class="search_booking region ">        	
		        	<h3><a style="color:#FFF;" href="#">Votre recherche</a></h3><br><br>		        	
		        	<div  class="form_match">
		       			<form  name="f" action="{{ path('arenafootball_plateform_search_match') }}" method="POST">
		       					<table style="border:none;">
		       					{#<tr>
		       					<td style="background:#333; border-radius:5px; width:15%; text-align:center; color:#FFF;"><b style="font-size: 20px;" class="icon-location"> </b></td>
		       					<td>		       					
		       						<select name="region" >
	                    						<option style="background:#ccc;" value="" >Choisissez un région...</option>
	                    						{% for item in regioncentre %}	
	                    							<option value="{{ item.id }}">{{ item.nom }}</option>
		       									{% endfor %}	                    						
	                    			</select>
		       					</td>
		       					</tr>#}
		       					<tr>
									<td style="background:#333; border-radius:5px; width:15%; text-align:center; color:#FFF;"><b style="font-size: 20px;" class="icon-office"> </b></td>
									<td>
										<select name="centre" required>	                    						
	                    						{% for item in centre %}	
	                    							<option  style="background:#ccc;" value="{{ item.id }}">{{ item.nom }} - {{ item.region }}</option>
		       									{% endfor %}
		       									{% for item in cent %}	
	                    							<option value="{{ item.id }}">{{ item.nom }} - {{ item.region }}</option>
		       									{% endfor %}	                    						
	                    			</select>
									</td>
								</tr>										       							       				
								<tr>
									<td style="background:#333; border-radius:5px; text-align:center; color:#FFF;"><b style="font-size: 20px;" class="icon-calendar"> </b></td>
									<td><input type="date" id="datetime" name="date_book" value="{{ datebook|date('d-m-Y') }}" pattern="(0[1-9]|1[0-9]|2[0-9]|3[01]).(0[1-9]|1[012]).[0-9]{4}" required/></td>
								</tr>
								<tr>
									<td>&nbsp;</td><td>&nbsp;</td>
								</tr>								
								
								<tr>					
								<td colspan="2">
									<aside style="text-align:center;">
										<button type="submit"><span class="icon-search"> </span> Recherche</button>
									</aside>
								</td>
								</tr>								
							</table>	
						</form><br>
		       		</div> 
		    </div>